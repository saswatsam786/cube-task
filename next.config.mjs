/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', "plus.unsplash.com"], // Add Unsplash domain here
    },
};

export default nextConfig;
